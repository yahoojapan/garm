apiVersion: v1
kind: Service
metadata:
  name: {{ template "garm.fullname" . }}
  labels:
    app: {{ template "garm.name" . }}
    chart: {{ template "garm.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "garm.name" . }}
    release: {{ .Release.Name }}
